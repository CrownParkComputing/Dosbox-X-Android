# Release signing config for Retro-Dosbox.
#
# Copy this file to `key.properties` (same directory) and fill in the real
# values. `key.properties` is gitignored; the example is checked in so the
# schema survives across checkouts.
#
# These are the same four values the legacy Java app used. The keystore is
# the upload keystore already registered with Play Console under the
# `com.dosboxx.app` listing -- keeping the signing key means Retro-Dosbox
# ships as an update of the existing Play Store listing, not a brand-new
# app, so existing users receive it automatically and reviews / install
# counts carry over. See `android/app/build.gradle.kts` for how they're
# consumed.
#
# When `key.properties` is absent (the default on a fresh checkout), the
# release build falls back to the debug keystore and prints a warning -- so
# `flutter run --release` still works in dev. Real release builds for the
# Play Store require this file.
storeFile=/absolute/path/to/your-upload-keystore.jks
storePassword=your-keystore-password
keyAlias=your-key-alias
keyPassword=your-key-password