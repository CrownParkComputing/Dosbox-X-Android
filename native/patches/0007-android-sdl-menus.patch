diff --git a/include/menu.h b/include/menu.h
index 28666f83..d989bb99 100644
--- a/include/menu.h
+++ b/include/menu.h
@@ -102,7 +102,14 @@ void SetVal(const std::string & secname, const std::string & preval, const std::
 #define DOSBOXMENU_NSMENU   (2)     /* Mac OS X NSMenu / NSMenuItem resources */
 #define DOSBOXMENU_SDLDRAW  (3)     /* menus that WE draw on the SDL surface */
 
-#if C_FORCE_MENU_SDLDRAW /* Programmer/Dev wants to compile with SDL drawn menus even if host OS offers menus (shrug) Ok */
+#if defined(__ANDROID__)
+/* Android has no host menu bar to borrow, so use the menus DOSBox-X draws
+ * itself onto the SDL surface. Geek mode turns them on (showmenu=true) to
+ * get the full DOSBox-X menu system on the device; beginner mode leaves
+ * showmenu=false and the bar never appears. The surface sizing patch
+ * already reserves menuheight above the DOS area for exactly this. */
+# define DOSBOXMENU_TYPE    DOSBOXMENU_SDLDRAW
+#elif C_FORCE_MENU_SDLDRAW /* Programmer/Dev wants to compile with SDL drawn menus even if host OS offers menus (shrug) Ok */
 # define DOSBOXMENU_TYPE    DOSBOXMENU_SDLDRAW
 #elif defined(HX_DOS)
 # define DOSBOXMENU_TYPE    DOSBOXMENU_SDLDRAW
