diff --git a/configure.ac b/configure.ac
index 332e6bfe5..cea9975b8 100644
--- a/configure.ac
+++ b/configure.ac
@@ -713,6 +713,17 @@ case "$host" in
        dnl FEATURE: Whether to support direct serial port pass-through
        AC_DEFINE(C_DIRECTSERIAL, 1, [ Define to 1 if you want serial pass-through support (Win32, Posix and OS/2).])
        ;;
+    *-*-android*)
+       dnl Android is Linux-kernel based, but bionic has no librt (shm_*/clock_*
+       dnl live in libc) and has no parallel/serial port pass-through. Keep the
+       dnl LINUX feature define so the POSIX code paths build, but drop the
+       dnl Linux-desktop-only bits. Added by the Android port (native/patches).
+       AC_DEFINE(LINUX, 1, [Compiling on GNU/Linux])
+       AC_DEFINE(ANDROID, 1, [Compiling on Android])
+       CXXFLAGS="$CXXFLAGS -D_XOPEN_SOURCE=700 -D_POSIX_C_SOURCE=200809L"
+       dnl FEATURE: Whether to support direct serial port pass-through
+       AC_DEFINE(C_DIRECTSERIAL, 1, [ Define to 1 if you want serial pass-through support (Win32, Posix and OS/2).])
+       ;;
     *-*-linux*)
        AC_DEFINE(LINUX, 1, [Compiling on GNU/Linux])
        CXXFLAGS="$CXXFLAGS -D_XOPEN_SOURCE=700 -D_POSIX_C_SOURCE=200809L"
diff --git a/src/gui/render.cpp b/src/gui/render.cpp
index df45f1bf5..32cbeb48c 100644
--- a/src/gui/render.cpp
+++ b/src/gui/render.cpp
@@ -1356,6 +1356,11 @@ void RENDER_OnSectionPropChange(Section *x) {
     if (s_aspect == "nearest") render.aspect = ASPECT_NEAREST;
     if (s_aspect == "bilinear") render.aspect = ASPECT_BILINEAR;
 #endif
+#if defined(__ANDROID__)
+    /* The GPU aspect-fits the content surface to the display (see the vendored
+       SDL2 SDL_video.c). Never run the CPU aspect/postrender scaler on Android. */
+    render.aspect = ASPECT_FALSE;
+#endif
 
     render.frameskip.max = (Bitu)section->Get_int("frameskip");
 
diff --git a/src/gui/sdl_gui.cpp b/src/gui/sdl_gui.cpp
index ec2af889f..31f45ee8a 100644
--- a/src/gui/sdl_gui.cpp
+++ b/src/gui/sdl_gui.cpp
@@ -406,14 +406,14 @@ static GUI::ScreenSDL *UI_Startup(GUI::ScreenSDL *screen) {
             uint32_t *bg = (uint32_t*)((unsigned int)(y+sy)*(unsigned int)background->pitch + (char*)background->pixels) + sx;
             for (int x = 0; x < sw_draw; x++) {
                 int r = 0, g = 0, b = 0;
-                getPixel(x    *(int)render.src.width/sw, y    *(int)render.src.height/sh, r, g, b, 3); 
-                getPixel((x-1)*(int)render.src.width/sw, y    *(int)render.src.height/sh, r, g, b, 3); 
-                getPixel(x    *(int)render.src.width/sw, (y-1)*(int)render.src.height/sh, r, g, b, 3); 
-                getPixel((x-1)*(int)render.src.width/sw, (y-1)*(int)render.src.height/sh, r, g, b, 3); 
-                getPixel((x+1)*(int)render.src.width/sw, y    *(int)render.src.height/sh, r, g, b, 3); 
-                getPixel(x    *(int)render.src.width/sw, (y+1)*(int)render.src.height/sh, r, g, b, 3); 
-                getPixel((x+1)*(int)render.src.width/sw, (y+1)*(int)render.src.height/sh, r, g, b, 3); 
-                getPixel((x-1)*(int)render.src.width/sw, (y+1)*(int)render.src.height/sh, r, g, b, 3); 
+                getPixel(x    *(int)render.src.width/sw, y    *(int)render.src.height/sh, r, g, b, 3);
+                getPixel((x-1)*(int)render.src.width/sw, y    *(int)render.src.height/sh, r, g, b, 3);
+                getPixel(x    *(int)render.src.width/sw, (y-1)*(int)render.src.height/sh, r, g, b, 3);
+                getPixel((x-1)*(int)render.src.width/sw, (y-1)*(int)render.src.height/sh, r, g, b, 3);
+                getPixel((x+1)*(int)render.src.width/sw, y    *(int)render.src.height/sh, r, g, b, 3);
+                getPixel(x    *(int)render.src.width/sw, (y+1)*(int)render.src.height/sh, r, g, b, 3);
+                getPixel((x+1)*(int)render.src.width/sw, (y+1)*(int)render.src.height/sh, r, g, b, 3);
+                getPixel((x-1)*(int)render.src.width/sw, (y+1)*(int)render.src.height/sh, r, g, b, 3);
                 int r1, g1, b1;
 #if defined(USE_TTF)
                 if (ttf.inUse && confres) {
@@ -444,7 +444,7 @@ static GUI::ScreenSDL *UI_Startup(GUI::ScreenSDL *screen) {
                 }
                 bg[x] = ((unsigned int)r1 << (unsigned int)rs) |
                     ((unsigned int)g1 << (unsigned int)gs) |
-                    ((unsigned int)b1 << (unsigned int)bs); 
+                    ((unsigned int)b1 << (unsigned int)bs);
             }
         }
     }
@@ -472,14 +472,14 @@ static GUI::ScreenSDL *UI_Startup(GUI::ScreenSDL *screen) {
         SDL_Event event;
         SDL_SetSurfaceBlendMode(screenshot, SDL_BLENDMODE_BLEND);
         for (int i = 0xff; i > 0; i -= 0x40) {
-            SDL_SetSurfaceAlphaMod(screenshot, i); 
-            SDL_BlitSurface(background, NULL, sdlscreen, NULL); 
+            SDL_SetSurfaceAlphaMod(screenshot, i);
+            SDL_BlitSurface(background, NULL, sdlscreen, NULL);
             SDL_BlitSurface(screenshot, NULL, sdlscreen, NULL);
             SDL_Window* GFX_GetSDLWindow(void);
             SDL_UpdateWindowSurface(GFX_GetSDLWindow());
-            while (SDL_PollEvent(&event)); 
-            SDL_Delay(40); 
-        } 
+            while (SDL_PollEvent(&event));
+            SDL_Delay(40);
+        }
         SDL_SetSurfaceBlendMode(screenshot, SDL_BLENDMODE_NONE);
     }
 #else
@@ -489,24 +489,24 @@ static GUI::ScreenSDL *UI_Startup(GUI::ScreenSDL *screen) {
     if (screenshot != NULL && background != NULL) {
         // fade out
         // Jonathan C: do it FASTER!
-        SDL_Event event; 
+        SDL_Event event;
         for (int i = 0xff; i > 0; i -= 0x40) {
-            SDL_SetAlpha(screenshot, SDL_SRCALPHA, i); 
-            SDL_BlitSurface(background, NULL, sdlscreen, NULL); 
-            SDL_BlitSurface(screenshot, NULL, sdlscreen, NULL); 
-            SDL_UpdateRect(sdlscreen, 0, 0, 0, 0); 
-            while (SDL_PollEvent(&event)); 
-            SDL_Delay(40); 
+            SDL_SetAlpha(screenshot, SDL_SRCALPHA, i);
+            SDL_BlitSurface(background, NULL, sdlscreen, NULL);
+            SDL_BlitSurface(screenshot, NULL, sdlscreen, NULL);
+            SDL_UpdateRect(sdlscreen, 0, 0, 0, 0);
+            while (SDL_PollEvent(&event));
+            SDL_Delay(40);
         }
     }
 #endif
- 
+
     if (screenshot != NULL && background != NULL)
         SDL_BlitSurface(background, NULL, sdlscreen, NULL);
 #if defined(C_SDL2)
     SDL_Window* GFX_GetSDLWindow(void);
     SDL_UpdateWindowSurface(GFX_GetSDLWindow());
-#else   
+#else
     SDL_UpdateRect(sdlscreen, 0, 0, 0, 0);
 #endif
 
@@ -611,14 +611,14 @@ static void UI_Shutdown(GUI::ScreenSDL *screen) {
         SDL_Event event;
         SDL_SetSurfaceBlendMode(screenshot, SDL_BLENDMODE_BLEND);
         for (unsigned int i = 0x00; i < 0xff; i += 0x60) {
-            SDL_SetSurfaceAlphaMod(screenshot, i); 
-            SDL_BlitSurface(background, NULL, sdlscreen, NULL); 
+            SDL_SetSurfaceAlphaMod(screenshot, i);
+            SDL_BlitSurface(background, NULL, sdlscreen, NULL);
             SDL_BlitSurface(screenshot, NULL, sdlscreen, NULL);
             SDL_Window* GFX_GetSDLWindow(void);
             SDL_UpdateWindowSurface(GFX_GetSDLWindow());
-            while (SDL_PollEvent(&event)); 
-            SDL_Delay(40); 
-        } 
+            while (SDL_PollEvent(&event));
+            SDL_Delay(40);
+        }
         SDL_SetSurfaceBlendMode(screenshot, SDL_BLENDMODE_NONE);
     }
 
@@ -635,7 +635,7 @@ static void UI_Shutdown(GUI::ScreenSDL *screen) {
             SDL_BlitSurface(screenshot, NULL, sdlscreen, NULL);
             SDL_UpdateRect(sdlscreen, 0, 0, 0, 0);
             while (SDL_PollEvent(&event)) {}
-            SDL_Delay(40); 
+            SDL_Delay(40);
         }
     }
 #endif
@@ -1555,7 +1555,7 @@ public:
         Property *property;
         std::vector<Property*> properties;
         auto propertyIndex = 0;
-        
+
         while ((property = section->Get_prop(propertyIndex++)))
         {
             properties.push_back(property);
@@ -1565,7 +1565,7 @@ public:
         {
             return a->propname < b->propname;
         });
-        
+
         for(const auto & prop : properties)
         {
             if (!advopt->isChecked() && !prop->basic()) {i++;continue;}
@@ -3335,7 +3335,7 @@ public:
         bar->addItem(0,"");
         bar->addItem(0,MSG_Get("CLOSE"));
         bar->addMenu(MSG_Get("SETTINGS"));
-        
+
         // theme menu
         bar->addMenu(MSG_Get("THEME"));
         for (size_t ti=0;ti < (sizeof(theme_presets)/sizeof(theme_presets[0]));ti++) {
@@ -3346,7 +3346,7 @@ public:
 
             bar->addItem(2, theme_presets[ti].name);
         }
-        
+
         bar->addMenu(mainMenu.get_item("HelpMenu").get_text().c_str());
         bar->addItem(3,MSG_Get("VISIT_HOMEPAGE"));
         bar->addItem(3,"");
@@ -3368,10 +3368,10 @@ public:
         int i = 0;
 
         constexpr auto margin = 3;
-        
+
         const auto xSpace = gridbtnwidth + margin;
         const auto ySpace = gridbtnheight + margin;
-        
+
         std::function< std::pair<int,int>(const int) > gridfunc = [&/*access to locals here*/](const int i){
             return std::pair<int,int>(gridbtnx+(i%btnperrow)*xSpace, gridbtny+(i/btnperrow)*ySpace);
         };
@@ -3433,7 +3433,7 @@ public:
                           return false;
                       });
         }
-        
+
         for(const auto & sec : sections)
         {
             if (i != 0 && (i%16) == 0) bar->addItem(1, "|");
@@ -3589,14 +3589,14 @@ public:
             memset(path, 0, sizeof(path));
             memset(parambuffer, 0, sizeof(parambuffer));
             memset(params, 0, sizeof(params));
-            
+
             // We have to look in the OS/2 configuration for the default browser.
             // First step: Find the configuration files
             if (!PrfQueryProfile(hAnchor, &profile)) {
                 systemmessagebox(url.c_str(), "Could not query application handle", "ok", "error", 0);
                 return;
             }
-            
+
             // Second step: Open the configuration files and read exe path and parameters
             os2Ini = PrfOpenProfile(hAnchor, (PCSZ)userPath);
             if (os2Ini == NULLHANDLE) {
@@ -3611,12 +3611,12 @@ public:
 
             PrfQueryProfileString(os2Ini, (PCSZ)"WPURLDEFAULTSETTINGS", (PCSZ)"DefaultBrowserParameters", NULL, params, sizeof(params));
             PrfCloseProfile(os2Ini);
-            
+
             // concat arguments
-            if (strlen(params) > 0) 
+            if (strlen(params) > 0)
                 strncat(params, " ", 20);
             strncat(params, url.c_str(), url.length());
-            
+
             // Build parameter buffer
             strcpy(parambuffer, "Browser");
             paramptr = &parambuffer[strlen(parambuffer)+1];
@@ -3770,28 +3770,28 @@ static void UI_Select(GUI::ScreenSDL *screen, int select) {
             } break;
         case 2: {
             sec = control->GetSection("sdl");
-            section=static_cast<Section_prop *>(sec); 
+            section=static_cast<Section_prop *>(sec);
             auto *p = new SectionEditor(screen,50,30,section);
             p->raise();
             } break;
         case 3:
             sec = control->GetSection("dosbox");
-            section=static_cast<Section_prop *>(sec); 
+            section=static_cast<Section_prop *>(sec);
             new SectionEditor(screen,50,30,section);
             break;
         case 4:
             sec = control->GetSection("mixer");
-            section=static_cast<Section_prop *>(sec); 
+            section=static_cast<Section_prop *>(sec);
             new SectionEditor(screen,50,30,section);
             break;
         case 5:
             sec = control->GetSection("serial");
-            section=static_cast<Section_prop *>(sec); 
+            section=static_cast<Section_prop *>(sec);
             new SectionEditor(screen,50,30,section);
             break;
         case 6:
             sec = control->GetSection("ne2000");
-            section=static_cast<Section_prop *>(sec); 
+            section=static_cast<Section_prop *>(sec);
             new SectionEditor(screen,50,30,section);
             break;
         case 7:
@@ -3800,7 +3800,7 @@ static void UI_Select(GUI::ScreenSDL *screen, int select) {
             break;
         case 8:
             sec = control->GetSection("glide");
-            section=static_cast<Section_prop *>(sec); 
+            section=static_cast<Section_prop *>(sec);
             new SectionEditor(screen,50,30,section);
             break;
         case 9: {
@@ -4011,7 +4011,7 @@ static void UI_Select(GUI::ScreenSDL *screen, int select) {
 #if defined(C_SDL2)
         SDL_Window* GFX_GetSDLWindow(void);
         SDL_UpdateWindowSurface(GFX_GetSDLWindow());
-#else   
+#else
         SDL_UpdateRect(sdlscreen, 0, 0, 0, 0);
 #endif
         SDL_Delay(20);
@@ -4053,7 +4053,7 @@ void RunCfgTool(Bitu val) {
 #if C_OPENGL
         voodoo_ogl_update_dimensions();
 #endif
-    }    
+    }
 #if defined(MACOSX) && defined(C_SDL2) && C_METAL
     if(sdl.desktop.want_type == SCREEN_METAL){
         OUTPUT_Metal_Shutdown();
@@ -4061,10 +4061,16 @@ void RunCfgTool(Bitu val) {
         change_output(3);
 #endif
         change_output(14);
-    }  
+    }
 #endif
 }
 
+void GUI_Shortcut(int select) {
+    if(!select || running) return;
+#if defined(__ANDROID__)
+#include "SDL_system.h"
+#endif
+
 void GUI_Shortcut(int select) {
     if(!select || running) return;
 
@@ -4080,9 +4086,14 @@ void GUI_Shortcut(int select) {
         GFX_EndUpdate(nullptr);
         switchttf = true;
         PIC_AddEvent(RunCfgTool, 100);
-    } else
+        return;
+    }
 #endif
+#if defined(__ANDROID__)
+    SDL_AndroidSendMessage(0x8001, select);
+#else
     RunCfgTool(0);
+#endif
 }
 
 void GUI_Run(bool pressed) {
@@ -4095,7 +4106,12 @@ void GUI_Run(bool pressed) {
         GFX_EndUpdate(nullptr);
         switchttf = true;
         PIC_AddEvent(RunCfgTool, 100);
-    } else
+        return;
+    }
 #endif
+#if defined(__ANDROID__)
+    SDL_AndroidSendMessage(0x8001, 0);
+#else
     RunCfgTool(0);
+#endif
 }
diff --git a/src/gui/sdlmain.cpp b/src/gui/sdlmain.cpp
index c654f8853..5cce3f7b4 100644
--- a/src/gui/sdlmain.cpp
+++ b/src/gui/sdlmain.cpp
@@ -1116,6 +1116,137 @@ bool IsDebuggerActive(void);
 
 extern std::string dosbox_title;
 
+#if defined(__ANDROID__)
+#include <jni.h>
+/* DOSBox-X's status line (incl. "FPS nn, nn%/RT" when showdetails=true) would go
+   in the window title; on Android there is no title bar, so cache it here for the
+   Java-side FPS overlay (com.dosboxx.app.DosStatus.getStatusLine, a native
+   method — must exist or the overlay throws UnsatisfiedLinkError). */
+char android_status_line[256] = {0};
+extern "C" JNIEXPORT jstring JNICALL
+Java_com_dosboxx_app_DosStatus_getStatusLine(JNIEnv *env, jclass clazz) {
+    (void)clazz;
+    return env->NewStringUTF(android_status_line);
+}
+
+#include <sstream>
+#include <vector>
+
+static std::string json_escape(const std::string& s) {
+    std::stringstream ss;
+    for (size_t i = 0; i < s.length(); ++i) {
+        char c = s[i];
+        if (c == '"') ss << "\\\"";
+        else if (c == '\\') ss << "\\\\";
+        else if (c == '\b') ss << "\\b";
+        else if (c == '\f') ss << "\\f";
+        else if (c == '\n') ss << "\\n";
+        else if (c == '\r') ss << "\\r";
+        else if (c == '\t') ss << "\\t";
+        else if ((unsigned char)c < 32) {
+            char buf[8];
+            snprintf(buf, sizeof(buf), "\\u%04x", (int)c);
+            ss << buf;
+        } else ss << c;
+    }
+    return ss.str();
+}
+
+extern "C" JNIEXPORT jobjectArray JNICALL
+Java_com_dosboxx_app_DosConfig_getSections(JNIEnv *env, jclass clazz) {
+    (void)clazz;
+    std::vector<std::string> names;
+    for (int i = 0; ; ++i) {
+        Section* s = control->GetSection(i);
+        if (!s) break;
+        names.push_back(s->GetName());
+    }
+    jclass stringClass = env->FindClass("java/lang/String");
+    jobjectArray array = env->NewObjectArray((jsize)names.size(), stringClass, NULL);
+    for (size_t i = 0; i < names.size(); ++i) {
+        env->SetObjectArrayElement(array, (jsize)i, env->NewStringUTF(names[i].c_str()));
+    }
+    return array;
+}
+
+extern "C" JNIEXPORT jstring JNICALL
+Java_com_dosboxx_app_DosConfig_getSectionProperties(JNIEnv *env, jclass clazz, jstring sectionName) {
+    (void)clazz;
+    const char* nativeSectionName = env->GetStringUTFChars(sectionName, 0);
+    Section* s = control->GetSection(nativeSectionName);
+    env->ReleaseStringUTFChars(sectionName, nativeSectionName);
+
+    if (!s) return env->NewStringUTF("[]");
+
+    Section_prop* sp = dynamic_cast<Section_prop*>(s);
+    if (!sp) return env->NewStringUTF("[]");
+
+    std::stringstream ss;
+    ss << "[";
+    bool first = true;
+    for (int i = 0; ; ++i) {
+        Property* p = sp->Get_prop(i);
+        if (!p) break;
+
+        if (!first) ss << ",";
+        first = false;
+
+        ss << "{";
+        ss << "\"name\":\"" << json_escape(p->propname) << "\",";
+        ss << "\"type\":" << (int)p->Get_type() << ",";
+        ss << "\"value\":\"" << json_escape(p->GetValue().ToString()) << "\",";
+        ss << "\"default\":\"" << json_escape(p->Get_Default_Value().ToString()) << "\",";
+        ss << "\"help\":\"" << json_escape(p->Get_help() ? p->Get_help() : "") << "\",";
+        ss << "\"values\":[";
+        const std::vector<Value>& vals = p->GetValues();
+        for (size_t j = 0; j < vals.size(); ++j) {
+            if (j > 0) ss << ",";
+            ss << "\"" << json_escape(vals[j].ToString()) << "\"";
+        }
+        ss << "]}";
+    }
+    ss << "]";
+    return env->NewStringUTF(ss.str().c_str());
+}
+
+extern "C" JNIEXPORT jboolean JNICALL
+Java_com_dosboxx_app_DosConfig_setProperty(JNIEnv *env, jclass clazz, jstring sectionName, jstring propName, jstring value) {
+    (void)clazz;
+    const char* ns = env->GetStringUTFChars(sectionName, 0);
+    const char* np = env->GetStringUTFChars(propName, 0);
+    const char* nv = env->GetStringUTFChars(value, 0);
+
+    Section* s = control->GetSection(ns);
+    bool ok = false;
+    if (s) {
+        Section_prop* sp = dynamic_cast<Section_prop*>(s);
+        if (sp) {
+            Property* p = sp->Get_prop(np);
+            if (p) {
+                ok = p->SetValue(nv);
+                if (ok) {
+                    for (std::list<SectionFunction>::iterator i = sp->onpropchange.begin(); i != sp->onpropchange.end(); ++i) {
+                        (*i)(sp);
+                    }
+                }
+            }
+        }
+    }
+
+    env->ReleaseStringUTFChars(sectionName, ns);
+    env->ReleaseStringUTFChars(propName, np);
+    env->ReleaseStringUTFChars(value, nv);
+    return ok;
+}
+
+extern "C" JNIEXPORT jboolean JNICALL
+Java_com_dosboxx_app_DosConfig_saveConfig(JNIEnv *env, jclass clazz) {
+    (void)clazz;
+    if (control->configfiles.empty()) return false;
+    return control->PrintConfig(control->configfiles[0].c_str());
+}
+#endif
+
 void GFX_SetTitle(int32_t cycles, int frameskip, Bits timing, bool paused) {
     (void)frameskip;//UNUSED
     (void)timing;//UNUSED
@@ -1186,6 +1317,11 @@ void GFX_SetTitle(int32_t cycles, int frameskip, Bits timing, bool paused) {
 #if C_DEBUG
     if (IsDebuggerActive()) strcat(title," DEBUGGER");
 #endif
+#if defined(__ANDROID__)
+    /* expose the finished status line to the Java FPS overlay */
+    strncpy(android_status_line, title, sizeof(android_status_line)-1);
+    android_status_line[sizeof(android_status_line)-1] = 0;
+#endif
 #if defined(C_SDL2)
     SDL_SetWindowTitle(sdl.window,title);
 #else
@@ -1225,11 +1361,11 @@ std::string replaceNewlineWithEscaped(const std::string& input) {
             output += "'\\''";
         }
         else if (input[i] == '\n'){
-            output += "\\n";   // '\n' needs to be replaced to "\\n" 
+            output += "\\n";   // '\n' needs to be replaced to "\\n"
             i++;
         }
         //else if (input[i] == '\"'){
-        //    output += "\\\"";   // '"' needs to be replaced to "\\\"" 
+        //    output += "\\\"";   // '"' needs to be replaced to "\\\""
         //    i++;
         //}
         else {
@@ -1245,18 +1381,18 @@ std::string replaceNewlineWithEscaped(const std::string& input) {
 
     while(i < input.length()) {
         if(input[i] == '\\') {
-            if(input[i + 1] != 'n') output += '\\'; // "\n" needs to be escaped to "\\n" 
+            if(input[i + 1] != 'n') output += '\\'; // "\n" needs to be escaped to "\\n"
         }
         else if(input[i] == '\n') {
-            output += "\\\\n";   // '\n' needs to be replaced to "\\n" 
+            output += "\\\\n";   // '\n' needs to be replaced to "\\n"
             i++;
         }
         else if(input[i] == '`') {
-            output += "\\`";  // '`' needs to be replaced to "\\`" 
+            output += "\\`";  // '`' needs to be replaced to "\\`"
             i++;
         }
         else if(input[i] == '"') {
-            output += "\\\"";  // '"' needs to be replaced to "\\\"" 
+            output += "\\\"";  // '"' needs to be replaced to "\\\""
             i++;
         }
         else {
@@ -1292,9 +1428,9 @@ bool systemmessagebox(char const * aTitle, char const * aMessage, char const * a
     size_t aTitleLength = strlen(aTitle);
     char* lMessage = (char*)malloc((aMessageLength * 2 + 1) * sizeof(char));  // DBCS may expand to 3 to 4 bytes when converted to UTF-8
     char* lTitle = (char*)malloc((aTitleLength * 2 + 1) * sizeof(char));  // DBCS may expand to 3 to 4 bytes when converted to UTF-8
-    lDialogString = replaceNewlineWithEscaped(lDialogString); // String may include "\n" which needs to be escaped to "\\n" 
+    lDialogString = replaceNewlineWithEscaped(lDialogString); // String may include "\n" which needs to be escaped to "\\n"
     CodePageGuestToHostUTF8(lMessage, lDialogString.c_str());
-    lTitleString = replaceNewlineWithEscaped(lTitleString); // String may include "\n" which needs to be escaped to "\\n" 
+    lTitleString = replaceNewlineWithEscaped(lTitleString); // String may include "\n" which needs to be escaped to "\\n"
     CodePageGuestToHostUTF8(lTitle, lTitleString.c_str());
     bool result=tinyfd_messageBox(lTitle, lMessage, aDialogType, aIconType, aDefaultButton);
     free(lMessage);
@@ -1791,6 +1927,19 @@ SDL_Window* GFX_GetSDLWindow(void) {
 SDL_Window* GFX_SetSDLWindowMode(uint16_t width, uint16_t height, SCREEN_TYPES screenType)
 {
     static SCREEN_TYPES lastType = SCREEN_SURFACE;
+#if defined(__ANDROID__)
+    /* The Android SDL window is always the full display; keep the window/renderer
+       (and thus the GL viewport) at display size. The small DOS framebuffer is
+       handled as a content-sized texture scaled on the GPU (see output_surface +
+       SDL_DBX_SetContentSize), not by shrinking the window. */
+    {
+        SDL_DisplayMode dm;
+        if (SDL_GetDesktopDisplayMode(0, &dm) == 0 && dm.w > 0 && dm.h > 0) {
+            width = (uint16_t)dm.w;
+            height = (uint16_t)dm.h;
+        }
+    }
+#endif
     if (sdl.renderer) {
         SDL_DestroyRenderer(sdl.renderer);
         sdl.renderer = nullptr;
@@ -8641,6 +8790,14 @@ int main(int argc, char* argv[]) SDL_MAIN_NOEXCEPT {
     int workdirsave = 0;
     std::string workdirsaveas = "";
 
+#if defined(__ANDROID__)
+    /* Android has no interactive folder picker (no zenity/kdialog; tinyfd would
+       shell out and hang at 100% CPU when isatty(0)==false and cwd=="/", both
+       true on Android). Never prompt. All prompt logic below is gated on
+       opt_promptfolder < 0, so forcing 0 here disables it entirely. */
+    control->opt_promptfolder = 0;
+#endif
+
 #if defined(MACOSX) || defined(LINUX) || (defined(WIN32) && !defined(HX_DOS))
     {
         if(control->opt_promptfolder < 0) {
@@ -9358,8 +9515,11 @@ int main(int argc, char* argv[]) SDL_MAIN_NOEXCEPT {
         else {
             const char* env_driver = getenv("SDL_VIDEODRIVER");
             if(!env_driver || !*env_driver) {
-#if defined(LINUX)
+#if defined(LINUX) && !defined(__ANDROID__)
                 putenv("SDL_VIDEODRIVER=x11"); // default to x11 (xwayland) on Linux
+#elif defined(__ANDROID__)
+                /* let SDL auto-pick its "android" video driver; forcing x11 here
+                   makes SDL_Init fail with "x11 not available" */
 #elif defined(WIN32)
                 /* hack: Encourage SDL to use windib if not otherwise specified */
 #if defined(C_SDL2)
@@ -9567,6 +9727,27 @@ int main(int argc, char* argv[]) SDL_MAIN_NOEXCEPT {
         }
 #endif
 
+#if defined(__ANDROID__)
+        /* Load the launcher-written config from app-external storage. The workdir
+           logic above calls control->ClearExtraData(), which wipes every section's
+           data (including [autoexec]); parse our conf here, right before
+           GUI_StartUp, so the launcher's "mount c ... / run game" autoexec
+           actually executes instead of dropping to a bare Z:\> prompt. */
+        {
+            const char *extpath = SDL_AndroidGetExternalStoragePath();
+            if (extpath != NULL) {
+                std::string androidconf = std::string(extpath) + "/dosbox-x.conf";
+                struct stat ast;
+                if (stat(androidconf.c_str(), &ast) == 0 && S_ISREG(ast.st_mode)) {
+                    LOG_MSG("ANDROID: loading launcher config %s", androidconf.c_str());
+                    control->ParseConfigFile(androidconf.c_str());
+                } else {
+                    LOG_MSG("ANDROID: no launcher config at %s", androidconf.c_str());
+                }
+            }
+        }
+#endif
+
         /* GUI init */
         GUI_StartUp();
 
diff --git a/src/output/output_surface.cpp b/src/output/output_surface.cpp
index 29439167d..310748590 100644
--- a/src/output/output_surface.cpp
+++ b/src/output/output_surface.cpp
@@ -51,6 +51,13 @@ void OUTPUT_SURFACE_Select()
 #endif
 }
 
+#if defined(C_SDL2) && defined(__ANDROID__)
+/* Implemented in the vendored SDL2 (vs/sdl2/src/video/SDL_video.c): size the
+   window framebuffer to the DOS content and GPU-scale it to the full display. */
+extern "C" void SDL_DBX_SetContentSize(int w, int h);
+extern "C" void SDL_DBX_InvalidateWindowSurface(SDL_Window *window);
+#endif
+
 #if defined(C_SDL2)
 Bitu OUTPUT_SURFACE_SetSize()
 {
@@ -130,6 +137,26 @@ Bitu OUTPUT_SURFACE_SetSize()
         }
 #endif
 
+#if defined(__ANDROID__)
+        /* Android: render DOS video (+ the menu bar) into a content-sized
+           surface; the vendored SDL2 then aspect-fits that whole surface onto
+           the full display on the GPU (no per-frame CPU upscale). The window
+           itself stays full-display size (see GFX_SetSDLWindowMode). */
+        int final_width  = (int)width;
+        int final_height = (int)height + (int)menuheight;
+        sdl.clip.x = 0;
+        sdl.clip.y = (int)menuheight;          /* DOS area sits below the menu bar */
+        sdl.clip.w = (int)width;
+        sdl.clip.h = (int)height;
+        SDL_DBX_SetContentSize(final_width, final_height);
+        LOG_MSG("android surface content=%dx%d (menuh=%d)", final_width, final_height, (int)menuheight);
+        sdl.window = GFX_SetSDLWindowMode(final_width, final_height, SCREEN_SURFACE);
+        if (sdl.window == NULL)
+            E_Exit("Could not set windowed video mode %ix%i: %s", (int)sdl.draw.width, (int)sdl.draw.height, SDL_GetError());
+        /* drop the stale (early splash) window-sized framebuffer so the next
+           SDL_GetWindowSurface rebuilds it at the new content size */
+        SDL_DBX_InvalidateWindowSurface(sdl.window);
+#else
         /* decide where the rectangle on the screen goes */
         int final_width,final_height;
 
@@ -189,6 +216,7 @@ Bitu OUTPUT_SURFACE_SetSize()
             if (sdl.window == NULL)
                 E_Exit("Could not set windowed video mode %ix%i: %s", (int)sdl.draw.width, (int)sdl.draw.height, SDL_GetError());
         }
+#endif /* !__ANDROID__ */
     }
     sdl.surface = SDL_GetWindowSurface(sdl.window);
     if (sdl.surface == NULL)
diff --git a/vs/sdl/src/cdrom/linux/SDL_syscdrom.c b/vs/sdl/src/cdrom/linux/SDL_syscdrom.c
index 3cc02315c..a7017cb96 100644
--- a/vs/sdl/src/cdrom/linux/SDL_syscdrom.c
+++ b/vs/sdl/src/cdrom/linux/SDL_syscdrom.c
@@ -32,7 +32,7 @@
 #include <fcntl.h>
 #include <errno.h>
 #include <unistd.h>
-#ifdef __LINUX__
+#if defined(__LINUX__) || defined(__ANDROID__) || defined(__linux__)
 #ifdef HAVE_LINUX_VERSION_H
 /* linux 2.6.9 workaround */
 #include <linux/version.h>
diff --git a/vs/sdl2/src/video/SDL_video.c b/vs/sdl2/src/video/SDL_video.c
index 2f6c631d1..86c9aaa0f 100644
--- a/vs/sdl2/src/video/SDL_video.c
+++ b/vs/sdl2/src/video/SDL_video.c
@@ -227,6 +227,37 @@ static Uint32 SDL_DefaultGraphicsBackends(SDL_VideoDevice *_this)
     return 0;
 }
 
+/* === DOSBox-X Android: content-sized framebuffer + GPU aspect-fit scaling ===
+   The DOS framebuffer is small (e.g. 720x400); the Android window is the full
+   display size. We size the window-texture to the DOS *content*, then aspect-fit
+   that texture onto the full render output on the GPU at present time — no
+   per-frame CPU upscale. SDL builds with -fvisibility=hidden, so the entry
+   points DOSBox-X calls must be explicitly exported. */
+#define DBX_EXPORT __attribute__((visibility("default")))
+static int dbx_content_w = 0, dbx_content_h = 0;
+DBX_EXPORT void SDL_DBX_SetContentSize(int w, int h)
+{
+    dbx_content_w = w;
+    dbx_content_h = h;
+}
+static void DBX_GetFbSize(SDL_Window *window, int *w, int *h)
+{
+    if (dbx_content_w > 0 && dbx_content_h > 0) {
+        *w = dbx_content_w;
+        *h = dbx_content_h;
+        return;
+    }
+    SDL_GetWindowSizeInPixels(window, w, h);
+}
+DBX_EXPORT void SDL_DBX_InvalidateWindowSurface(SDL_Window *window)
+{
+    /* Drop the stale (window-sized) framebuffer so the next SDL_GetWindowSurface
+       rebuilds it at the current content size. */
+    if (window) {
+        window->surface_valid = SDL_FALSE;
+    }
+}
+
 static int SDL_CreateWindowTexture(SDL_VideoDevice *_this, SDL_Window *window, Uint32 *format, void **pixels, int *pitch)
 {
     SDL_RendererInfo info;
@@ -234,7 +265,7 @@ static int SDL_CreateWindowTexture(SDL_VideoDevice *_this, SDL_Window *window, U
     int i;
     int w, h;
 
-    SDL_GetWindowSizeInPixels(window, &w, &h);
+    DBX_GetFbSize(window, &w, &h);
 
     if (!data) {
         SDL_Renderer *renderer = NULL;
@@ -366,7 +397,7 @@ static int SDL_UpdateWindowTexture(SDL_VideoDevice *unused, SDL_Window *window,
     void *src;
     int w, h;
 
-    SDL_GetWindowSizeInPixels(window, &w, &h);
+    DBX_GetFbSize(window, &w, &h);
 
     data = SDL_GetWindowData(window, SDL_WINDOWTEXTUREDATA);
     if (!data || !data->texture) {
@@ -382,8 +413,30 @@ static int SDL_UpdateWindowTexture(SDL_VideoDevice *unused, SDL_Window *window,
             return -1;
         }
 
-        if (SDL_RenderCopy(data->renderer, data->texture, NULL, NULL) < 0) {
-            return -1;
+        if (dbx_content_w > 0 && dbx_content_h > 0) {
+            /* GPU aspect-fit the content texture into the real render output */
+            int ow = 0, oh = 0;
+            SDL_Rect dst;
+            SDL_GetRendererOutputSize(data->renderer, &ow, &oh);
+            if (ow > 0 && oh > 0 && w > 0 && h > 0) {
+                float sar = (float)w / (float)h;       /* content aspect */
+                float oar = (float)ow / (float)oh;     /* output aspect */
+                if (oar > sar) { dst.h = oh; dst.w = (int)((float)oh * sar + 0.5f); }
+                else           { dst.w = ow; dst.h = (int)((float)ow / sar + 0.5f); }
+                dst.x = (ow - dst.w) / 2;
+                dst.y = (oh - dst.h) / 2;
+            } else {
+                dst.x = 0; dst.y = 0; dst.w = w; dst.h = h;
+            }
+            SDL_SetRenderDrawColor(data->renderer, 0, 0, 0, 255);
+            SDL_RenderClear(data->renderer);
+            if (SDL_RenderCopy(data->renderer, data->texture, NULL, &dst) < 0) {
+                return -1;
+            }
+        } else {
+            if (SDL_RenderCopy(data->renderer, data->texture, NULL, NULL) < 0) {
+                return -1;
+            }
         }
 
         SDL_RenderPresent(data->renderer);
@@ -2715,7 +2768,7 @@ static SDL_Surface *SDL_CreateWindowFramebuffer(SDL_Window *window)
     SDL_bool created_framebuffer = SDL_FALSE;
     int w, h;
 
-    SDL_GetWindowSizeInPixels(window, &w, &h);
+    DBX_GetFbSize(window, &w, &h);
 
     /* This will switch the video backend from using a software surface to
        using a GPU texture through the 2D render API, if we think this would
